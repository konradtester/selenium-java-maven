package e2e.pages.locators;

import java.util.Map;

/**
 * Locators for the authenticated part of the application (dashboard, navigation, settings,
 * lists, etc). Intentionally empty in the starter -- add your own entries as you build out
 * scenarios, e.g. Map.of("Dashboard Header", "//h1[text()='Dashboard']").
 */
public final class AppLocators {

    private AppLocators() {
    }

    public static final Map<String, String> LOCATORS = Map.of();
}
