package e2e.pages.locators;

import java.util.Map;

/**
 * Locators for third-party integrations embedded in your app (payment gateways, widgets,
 * sandboxes). Empty by default -- example entries you might add:
 * Map.of("Stripe Card Number Field", "#card-number", "Stripe Pay Button", "#submit-stripe").
 */
public final class IntegrationsLocators {

    private IntegrationsLocators() {
    }

    public static final Map<String, String> LOCATORS = Map.of();
}
